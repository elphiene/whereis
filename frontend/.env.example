# Baked into the bundle at build time.
# Used in Traccar Client deep link during onboarding step 4:
#   traccar://connect?url=${VITE_SERVER_URL}/api&id={uuid}&period=60
# Changing this after users are onboarded requires them to redo device setup.
VITE_SERVER_URL=https://tracking.elphiene.com
